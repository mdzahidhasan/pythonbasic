from sys import argv
print (argv[1]+argv[2]) # Input will show as str format as all input data types are str format. here it will show concatanate of two string
print (int(argv[1])+ int(argv[2])) # here we converted our input to int format and now output will show the sum value.